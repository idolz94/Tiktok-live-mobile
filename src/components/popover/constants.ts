import { PopoverResolvedPlacement } from "./types";

export const DEFAULT_OFFSET = 8;
export const DEFAULT_SCREEN_PADDING = 12;
export const DEFAULT_ARROW_SIZE = 8;

export const PLACEMENT_FALLBACKS: PopoverResolvedPlacement[] = [
  "bottom",
  "top",
  "right",
  "left",
];
